packages:
  filesystem:
    version_source: "onlylinux"
    repository: "only-core"
    build_order: 1
    enabled: true

  zlib:
    version_source: "lfs"
    lfs_source_name: "zlib"
    arch_pkgbase: "zlib"
    repository: "only-core"
    build_order: 2
    enabled: true

  xz:
    version_source: "lfs"
    lfs_source_name: "xz"
    arch_pkgbase: "xz"
    repository: "only-core"
    build_order: 3
    enabled: true

  zstd:
    version_source: "lfs"
    lfs_source_name: "zstd"
    arch_pkgbase: "zstd"
    repository: "only-core"
    build_order: 4
    enabled: true

  bash:
    version_source: "lfs"
    lfs_source_name: "bash"
    arch_pkgbase: "bash"
    repository: "only-core"
    build_order: 5
    enabled: true

  coreutils:
    version_source: "lfs"
    lfs_source_name: "coreutils"
    arch_pkgbase: "coreutils"
    repository: "only-core"
    build_order: 6
    enabled: true
