# Generated manifests

This directory is reserved for metadata generated by the Only Linux build automation.

`lfs-packages.json` is created by `scripts/import_lfs.py` from the stable Linux From Scratch download metadata. Generated manifests are uploaded with GitHub Actions artifacts and are not maintained manually.
